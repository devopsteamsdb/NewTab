services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data.json:/app/data.json
      - ./static/img:/app/static/img
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
